name: Build and Release Windows C++ Project

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Build C++ Solution
      # This runs in default cmd so it has access to the msbuild command
      run: msbuild offgit/OffGit.slnx /p:Configuration=Release /p:Platform=x64

    - name: Stage Raw Executable
      # We target bash ONLY for this step so the wildcard expansion works
      shell: bash
      run: cp **/Release/OffGit.exe ./OffGit.exe

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        files: ./OffGit.exe
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
